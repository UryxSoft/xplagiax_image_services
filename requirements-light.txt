# ============================================================
# requirements-light.txt — versión ultra-ligera
#
# DIFERENCIA CLAVE vs requirements.txt original:
#   torch CPU-only → instalar con:
#   pip install -r requirements-light.txt \
#       --extra-index-url https://download.pytorch.org/whl/cpu
#
# Ahorro en disco: ~1.5 GB (torch CUDA vs CPU-only)
# Ahorro en RAM:   ~200 MB (sin drivers CUDA cargados)
# ============================================================

# Web framework
flask==3.0.3
gunicorn==22.0.0
gevent==24.2.1

# ML — CPU-only (se instala desde --extra-index-url pytorch/whl/cpu)
torch==2.3.1+cpu
torchvision==0.18.1+cpu
transformers==4.42.4
sentence-transformers==3.0.1
Pillow==10.4.0
numpy==1.26.4

# Quantización INT8 para CPU (incluida en torch, pero optimum la expone mejor)
optimum==1.21.2

# Vector DB
qdrant-client==1.10.1

# Cache & async jobs
redis==5.0.8
rq==1.16.2

# HTTP
requests==2.32.3
tenacity==8.3.0

# Observability
structlog==24.4.0
prometheus-client==0.20.0

# Type annotations
typing_extensions==4.12.2

# Security
cryptography==42.0.8