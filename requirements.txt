# ============================================================
# Production dependencies — pinned for reproducibility
# Run: pip install -r requirements.txt
# ============================================================

# Web framework
flask==3.0.3
gunicorn==22.0.0
gevent==24.2.1           # async worker for Gunicorn (non-blocking I/O)

# ML
torch==2.2.2             # CPU-only; for GPU: torch==2.3.1+cu121
torchvision==0.17.2
transformers==4.42.4
sentence-transformers==3.0.1
Pillow==10.4.0
numpy==1.26.4

# Vector DB
qdrant-client==1.10.1

# Cache & async jobs
redis==5.0.8
rq==1.16.2               # Redis Queue for async ML jobs

# HTTP
requests==2.32.3
tenacity==8.3.0          # retry with exponential backoff

# Storage — no extra dep needed; SeaweedFS uses plain HTTP (requests)

# Observability
structlog==24.4.0
prometheus-client==0.20.0
opentelemetry-api
opentelemetry-sdk
opentelemetry-exporter-otlp

# Security & Logic
imagehash

# Type annotations (stdlib in 3.11+, shim for 3.9/3.10)
typing_extensions==4.12.2

# Security (optional, for HMAC signature validation if needed later)
cryptography==42.0.8

# ============================================================
# Development / testing only (not installed in production)
# ============================================================
# pytest==8.2.2
# pytest-flask==1.3.0
# pytest-mock==3.14.0
# httpx==0.27.0
# coverage==7.6.0
# ruff==0.5.0
# mypy==1.10.0
